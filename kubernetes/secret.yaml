apiVersion: v1
kind: Secret
metadata:
  name: mern-app-secrets
type: Opaque
data:
  # These are example values - in production, use actual base64-encoded secrets
  # To create a base64 encoded string: echo -n "yourstring" | base64
  MONGODB_USER: YWRtaW4=  # admin
  MONGODB_PASSWORD: cGFzc3dvcmQ=  # password
